1. github搜索jsonpath 第一个java即可
2. @JsonView没有生效
3. chrome插件Restlet client
4. 处理错误页面BasicErrorController，
   @RequestMapping(produces = "text/html")
     请求头Accept中是否包含text/html判断是返回页面还是json数据
5. restfule api的拦截
   过滤器、拦截器、切片(aspect)